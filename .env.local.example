# ── FacultyHub (FCMS) — Supabase backend ─────────────────────────────────
# Create a Supabase project, apply supabase/schema.sql, then fill these in.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set to "false" to disable self-service registration.
NEXT_PUBLIC_FCMS_ALLOW_REGISTRATION=true
